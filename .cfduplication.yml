# 04_register_init.sh — самостоятельный standalone-скрипт, не подгружает модули и по этой причине
# содержит собственные копии функций, которые так же есть в файле 01_info_common.sh
# Исключаем 04_register_init.sh из анализа CodeFactor на дублирование кода
#
# install.sh — тоже standalone bootstrap-скрипт (curl | sh, модули ещё не на диске),
# verify_install_sha256() в нём — урезанный дубликат verify_github_sha256() из
# 00_fetch_with_mirrors.sh. Исключаем install.sh из анализа на дублирование кода
IgnorePatterns:
  - scripts/_xkeen/02_install/07_install_register/04_register_init.sh
  - install.sh
