{
  "routing": {
    "rules": [

// Российские домены через провайдера
      {
        "domain": [
          "xn--",
          "domain:su",
          "domain:ru",
          "domain:by",
          "domain:moscow",
          "yandex",
          "yastatic.net"
        ],
        "outboundTag": "direct"
      },

// Российские IP через провайдера
      {
        "ip": [
//          "ext:geoip_zkeenip.dat:ru",	// Перед использованием загрузите базу zkeenip.dat
          "188.124.45.105/30"		// https://pr-cy.ru/browser-details/ - проверка IP
        ],
        "outboundTag": "direct"
      },

// Остальной трафик через прокси
      {
        "network": "tcp,udp",
        "outboundTag": "vless-reality"
      }
    ]
  }
}